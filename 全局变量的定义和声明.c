//#include <stdio.h>
//#include <stdlib.h>
//#include "scope.h"
////  全局变量的定义只能定义一份的，可以声明多份的。
//int  a=3;  //  定义全局变量的。
////int e=a+2;  //全局变量的初始化必须要使用常量初始化的,会报错的。
////  int *p=(int *)malloc(sizeof(int)); 全局变量不要使用函数操作。
////  printf("%d\n",2);   不能使用函数定义全局变量的。全局的位置不要书写执行语句。
//int  main(void)
//{
//	fun(); //  执行的输出结果为3.C语言中变量的定义对应的是只能有一份的，但是声明的话是可以存在多个的声明的。
//	system("pause");
//	return  0;
//}